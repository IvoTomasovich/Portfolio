ScrollReveal().reveal('.section-content', {
    origin: 'bottom',   // Starting position
    distance: '20px',   // Distance moved
    duration: 1000,     // Animation duration
    delay: 300,         // Delay before reveal
    easing: 'ease-out', // Easing function
    interval: 200       // Interval between elements (if multiple)
});
